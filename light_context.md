# Контекст освітлення та координат у сцені

## Позиція камери

- Камера задана як: `eye = (0, 0, 3)`
- Точка, куди дивиться камера: `center = (0, 0, 0)`
- Отже напрямок погляду камери: уздовж `-Z`.

## Як читати координати світла `{x, y, z}`

Це вектор напряму світла у 3D-просторі.

- `x > 0` -> світло справа
- `x < 0` -> світло зліва
- `y > 0` -> світло зверху
- `y < 0` -> світло знизу
- `z > 0` -> світло більше "спереду" (у бік камери)
- `z < 0` -> світло більше "ззаду" (у бік затилка)

Поточний приклад у коді:

- `light_world = {-1.0, 0.35, 0.25}`
- Це означає: зліва, трохи зверху, трохи спереду.

## Орієнтири осей у сцені

- `+X` вправо, `-X` вліво
- `+Y` вгору, `-Y` вниз
- Камера стоїть на `+Z` і дивиться до `0`, тобто у напрямку `-Z`.

## Готові вектори для швидких тестів

- Чисто зліва: `{-1, 0, 0}`
- Зліва + зверху: `{-1, 0.5, 0}`
- Зліва + ззаду (для затилка): `{-1, 0.2, -0.6}`

## Нотатка

- Величини компонент (`1.0`, `0.35`, `0.25`) задають лише напрямок, бо в шейдері вектор світла нормалізується.
